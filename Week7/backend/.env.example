# Gemini API Key (required for image generation)
GEMINI_API_KEY=your_api_key_here

# AI Mode: "stub" or "real" (default: stub)
# stub: Use placeholder images (fast, no API cost)
# real: Use real Gemini AI (slow, costs money)
AI_MODE=stub

# AI Model (optional, default: gemini-2.5-flash-image)
# For image generation, recommended models:
#   - gemini-2.5-flash-image (faster, cheaper, free tier available)
#   - gemini-3-pro-image-preview (slower, higher quality, requires quota)
GEMINI_MODEL=gemini-2.5-flash-image

# AI Provider (optional, default: google)
# Options:
#   - google: Use Google Gemini API directly (requires proxy in some regions)
#   - openrouter: Use OpenRouter as proxy (no regional restrictions)
AI_PROVIDER=google

# OpenRouter Configuration (only needed if AI_PROVIDER=openrouter)
# Get your API key from: https://openrouter.ai/keys
OPENROUTER_API_KEY=your_openrouter_api_key_here
# Recommended models:
#   - google/gemini-2.5-flash-image (faster, cheaper)
#   - google/gemini-3-pro-image-preview (Nano Banana Pro, professional-grade text rendering)
OPENROUTER_MODEL=google/gemini-2.5-flash-image

# Image Generation Configuration
# IMAGE_SIZE: 1K (1024x576), 2K (2048x1152), 4K (4096x2304)
# Higher resolution = better quality but slower generation and larger files
# Recommended: 1K for development/testing, 2K for production
IMAGE_SIZE=1K

# IMAGE_ASPECT_RATIO: 16:9, 4:3, 1:1
# 16:9 is standard for presentation slides
IMAGE_ASPECT_RATIO=16:9

# Proxy Configuration (optional, only for google provider)
# Uncomment and set if you need to use a proxy to access Google APIs
# HTTP_PROXY=http://127.0.0.1:7890
# HTTPS_PROXY=http://127.0.0.1:7890
# NO_PROXY=localhost,127.0.0.1

# Server Configuration
HOST=0.0.0.0
PORT=8000
