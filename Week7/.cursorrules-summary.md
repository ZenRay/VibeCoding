# Cursor AI 规则文件创建完成

## ✅ 已创建文件

1. **后端规则**: `Week7/backend/.cursorrules` (283 行)
2. **前端规则**: `Week7/frontend/.cursorrules` (490 行)
3. **说明文档**: `Week7/CURSORRULES.md` (详细使用指南)

## 📋 规则内容概要

### 后端规则 (Python/FastAPI)

**涵盖领域**:
- ✅ 架构原则: SOLID/YAGNI/KISS/DRY
- ✅ 代码组织: API/Core/Data/Models 分层
- ✅ Python 最佳实践: 类型提示、PEP 8、Python 3.12+
- ✅ 错误处理: 具体异常 + structlog 日志
- ✅ 并发处理: async/await + 文件锁
- ✅ 环境管理: uv + venv 工作流
- ✅ FastAPI 特定: 依赖注入、请求验证、错误响应
- ✅ 测试指南: 单元测试和集成测试
- ✅ 代码审查清单

**核心要求**:
```python
# 类型提示
def update_slide(slide_id: str, text: str) -> Optional[Slide]:
    pass

# 原子操作
with file_lock(self.file_path):
    data = self._read_data()
    # ... 修改数据 ...
    self._write_data(data)

# 结构化日志
logger.info("slide_updated", slide_id=slide_id, text_length=len(text))

# 依赖注入
@router.get("/project")
async def get_project(store: YAMLStore = Depends(get_yaml_store)):
    return store.get_project_state()
```

### 前端规则 (TypeScript/React)

**涵盖领域**:
- ✅ 架构原则: SOLID (React 上下文) + YAGNI/KISS/DRY
- ✅ 代码组织: Components/Hooks/Utils/Types
- ✅ TypeScript 最佳实践: 严格类型、避免 any、泛型
- ✅ React 最佳实践: Hooks、Memoization、正确的依赖
- ✅ 状态管理: Zustand 模式
- ✅ 样式规范: Tailwind CSS 一致性
- ✅ 错误处理: Toast 通知 + 错误边界
- ✅ 性能优化: 代码分割、防抖
- ✅ 无障碍性: 语义化 HTML、键盘导航
- ✅ 测试指南: 组件测试

**核心要求**:
```typescript
// 明确类型
interface SlideEditorProps {
  slide: Slide;
  onUpdate: (text: string) => Promise<void>;
}

// Memoization
const handleUpdate = useCallback((text: string) => {
  return api.updateSlide(slide.id, { text });
}, [slide.id]);

// 完整依赖数组
useEffect(() => {
  fetchData();
}, [fetchData]); // 包含所有依赖

// 错误处理
try {
  await api.updateSlide(id, text);
  toast.success("Updated");
} catch (error) {
  toast.error("Failed to update");
  console.error(error);
}
```

## 🎯 规则特色

### 1. 最新技术栈
- **后端**: Python 3.12+, FastAPI 0.115+, Pydantic 2.10+
- **前端**: TypeScript 5.6+, React 19, Vite 6, Tailwind 4

### 2. 环境管理
- **后端**: 强制使用 `uv` 管理 `.venv`
- **前端**: npm/pnpm 管理依赖

### 3. 统一代码风格
- **后端**: PEP 8, snake_case, 类型提示
- **前端**: ESLint, camelCase, TypeScript strict mode

### 4. 最佳实践模式
- **后端**: 
  - 依赖注入 (FastAPI Depends)
  - 原子文件操作 (文件锁)
  - 结构化日志 (structlog)
  - async/await (I/O 操作)
- **前端**: 
  - 函数组件 + Hooks (不用类)
  - Zustand 状态管理
  - useMemo/useCallback (性能优化)
  - 语义化 HTML (无障碍)

## 📖 如何使用

### 方式 1: Subagent 自动应用

当使用 Cursor 的 Task 工具时，AI 会自动读取对应目录的规则：

```bash
# 后端任务会应用 backend/.cursorrules
/task "Implement Phase 2 backend endpoints" --cwd Week7/backend

# 前端任务会应用 frontend/.cursorrules
/task "Create StyleInitializer component" --cwd Week7/frontend
```

### 方式 2: 手动开发

在 Cursor IDE 中开发时：
- 在 `backend/` 目录下工作 → 自动应用后端规则
- 在 `frontend/` 目录下工作 → 自动应用前端规则

### 方式 3: 代码审查

提交前检查代码是否符合规则：
- **后端**: 阅读 `backend/.cursorrules` 的 "Code Review Checklist"
- **前端**: 阅读 `frontend/.cursorrules` 的 "Code Review Checklist"

## 🔍 验证规则生效

可以通过以下方式验证：

```bash
# 1. 查看规则文件
cat Week7/backend/.cursorrules
cat Week7/frontend/.cursorrules

# 2. 要求 AI 生成代码并检查
# 在 Cursor 中，在对应目录下创建新文件时，AI 会应用规则

# 3. 检查生成的代码是否包含:
# - 后端: 类型提示、依赖注入、错误处理
# - 前端: TypeScript 接口、Hooks、Memoization
```

## 📚 相关文档

- **详细说明**: `Week7/CURSORRULES.md`
- **项目 README**: `Week7/README.md`
- **技术计划**: `specs/001-ai-slide-generator/plan.md`
- **功能规范**: `specs/001-ai-slide-generator/spec.md`

## 🚀 下一步

规则已就绪，可以开始并行开发 Phase 2:
- **后端**: `backend/.cursorrules` 会指导 API 开发
- **前端**: `frontend/.cursorrules` 会指导 UI 组件开发

建议使用 Task 工具并行执行：
```bash
# 后端 subagent
/task "Implement Phase 2 backend (T008-T009)" --cwd Week7/backend

# 前端 subagent (并行)
/task "Implement Phase 2 frontend (T010-T012)" --cwd Week7/frontend
```
