# 数据库查询工具 - 后端

FastAPI 后端服务，提供数据库连接管理、元数据提取、SQL 查询和 AI SQL 生成功能。

## 安装

```bash
pip install uv
uv pip install -e .
```

## 运行

```bash
uvicorn app.main:app --reload --port 8000
```

## 测试

```bash
pytest
```

## API 文档

访问 http://localhost:8000/docs
