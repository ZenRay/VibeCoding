# Grafana 仪表盘配置
# 自动加载预定义仪表盘

apiVersion: 1

providers:
  - name: 'Project Alpha Dashboards'
    orgId: 1
    folder: ''
    folderUid: ''
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
